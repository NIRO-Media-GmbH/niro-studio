import type { ProjectConfig } from "../../../../core/types";

export const config: ProjectConfig = {
  format: "portrait",
  fps: 30,
  durationInSeconds: 8,
  transparent: false,
  clientSlug: "niro-demo",
  projectSlug: "launch-ad",
};
