#!/bin/sh
# Nach „git pull --rebase": wie post-merge; bei „git commit --amend" nichts. Blockiert nie.
skript="$(git rev-parse --show-toplevel)/tools/studio_abgleich.sh"
[ "$1" = "rebase" ] && [ -f "$skript" ] && sh "$skript" --nach-pull
exit 0
