#!/bin/sh
# Nach „git clone"/„git checkout <Branch>": NIRO-Grading-LUTs installieren (nicht bei Einzeldatei-Checkouts). Blockiert nie.
[ "$3" = "1" ] && sh "$(git rev-parse --show-toplevel)/tools/resolve/luts_sync.sh" --nur-installieren
exit 0
